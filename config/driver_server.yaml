# Falco driver server configuration
#
# server_url  — your custom driver server (flat file listing)
#   URL layout: {server_url}/falco_{distro}_{kernel_version}_{arch}.ko
#               {server_url}/falco_{distro}_{kernel_version}_{arch}.o
#   Leave empty to skip and go straight to fallback.
#
# fallback_url / listing_url / driver_version
#   Official Falco CloudFront CDN used when the custom server has no file.

server_url: http://localhost:8080

fallback_url: https://d20hasrqv82i0q.cloudfront.net
listing_url: https://falco-distribution.s3-eu-west-1.amazonaws.com
driver_version: "9.1.0+driver"
